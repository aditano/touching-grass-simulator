import { Vector3 } from "three";

export const TOUCH_SLOTS = 6;

/** Direction toward the sun, in front of the camera and a little to the right. */
export const SUN_DIR = new Vector3(0.42, 0.3, -0.58).normalize();
